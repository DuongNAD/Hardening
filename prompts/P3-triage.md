# Phase 3 — triage crash  (model: Gemini 3.6 Flash, effort High)

Đọc AGENTS.md rồi dùng skill crash-triage.

Artifact: `<đường dẫn từ hd_crashes hoặc logs/miri-*.log>`

Nhiệm vụ là PHÂN LOẠI và TÁI HIỆN, không phải fix. Fix nằm trong src/.
Ghi mục mới vào FINDINGS.md bằng hd_finding_add, rồi hd_verify (không tham số).
